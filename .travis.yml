language: node_js
node_js:
  - "stable"
cache:
  directories:
    - "node_modules"
script:
  - docker build -t imthekrish/deploy-hapi-hello-world .
  - docker run --name deploy-hapi-hello-world -p 80:3000 -d imthekrish/deploy-hapi-hello-world
